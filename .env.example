HOST=127.0.0.1
PORT=4100
DATA_FILE=./data/state.json
LOG_RETENTION=200

# Optional: OpenAI OAuth login import. Defaults match Codex CLI public flow.
# Changing the callback port requires an OAuth client that allows the same redirect_uri.
OPENAI_OAUTH_CLIENT_ID=app_EMoamEEZ73f0CkXaXp7hrann
OPENAI_OAUTH_AUTH_ENDPOINT=https://auth.openai.com/oauth/authorize
OPENAI_OAUTH_TOKEN_ENDPOINT=https://auth.openai.com/oauth/token
OPENAI_OAUTH_CALLBACK_PORT=1455
